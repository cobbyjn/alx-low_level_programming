#include <stdio.h>
/**
 * main - the main function
 *
 * Return: 0 when successful
 */
int main(void)
{
	char a;

	for (a = 'a' ; a <= 'z' ; a++)

		putchar(a);

	for (a = 'A' ; a <= 'Z' ; b++)

		putchar(b);

	putchar('\n');

	return (0);

}
